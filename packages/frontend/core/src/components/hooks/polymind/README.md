# PolyMind Hooks

> This directory will be moved to `@polymind/workspace/polymind/hooks` in the future.

Only put hooks in this directory if they are specific to PolyMind, for example
if they are using the PolyMind API, or if the `PolyMindWorkspace` is required.
